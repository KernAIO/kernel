Changesets: run `pnpm changeset` in a PR that changes a published package. On merge to `main`, CI publishes
prerelease versions to GitHub Packages (`@kernhq:registry=https://npm.pkg.github.com`); npmjs after public launch.
